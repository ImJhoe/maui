<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="CitasMedicasApp.Views.ConsultarCitasPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Consultar Citas Detalladas">

    <ScrollView Padding="20">
        <StackLayout Spacing="20">

            <Label Text="Consulta Detallada de Citas" 
                   FontSize="18" 
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

            <Label Text="Esta pantalla está en desarrollo..." 
                   FontSize="14" 
                   TextColor="Gray"
                   HorizontalOptions="Center" />

            <Button Text="Volver" 
                   Clicked="OnVolverClicked"
                   BackgroundColor="{AppThemeBinding Light=#757575, Dark=#616161}"
                   TextColor="White" />

        </StackLayout>
    </ScrollView>

</ContentPage>